/**
 * Login — placeholder (Wave 4.0 scaffold).
 * Wave 4.2: สร้าง login form จริง + Supabase auth + role check (owner/admin only).
 */
export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-surface">
      <div className="glass-shell rounded-lg p-8 w-full max-w-sm text-center">
        <h1 className="font-display text-2xl font-bold text-fg mb-2">
          AIKrub Admin
        </h1>
        <p className="text-fg-muted text-sm">
          ระบบ login กำลังสร้าง — Wave 4.2
        </p>
      </div>
    </div>
  );
}
